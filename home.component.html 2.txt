<div style="display: flex; justify-content: flex-end; align-items: center; height: 100vh;">
  <div class="col-md-4">
    <form (ngSubmit)="onSubmit()" [formGroup]="otherDataForm" class="form-group mb-3">
      <label for="width" class="mr-2">Dikdörtgenin genişliği (mm):</label>
      <input type="number" id="width" name="width" class="form-control form-control-sm" formControlName="width" />

      <label for="height" class="mr-2">Dikdörtgenin yüksekliği (mm):</label>
      <input type="number" id="height" name="height" class="form-control form-control-sm" formControlName="height" />

      <ng-container *ngIf="otherDataForm.controls['width'].value > 0 && otherDataForm.controls['height'].value > 0">
        <label for="horizontalLines" class="mr-2">Yatayda kaç adet çizgi olsun:</label>
        <input type="number" id="horizontalLines" name="horizontalLines" class="form-control form-control-sm" formControlName="horizontalLines" />

        <ng-container *ngIf="otherDataForm.controls['horizontalLines'].value > 0">
          <ng-container *ngFor="let horizontalLine of getLinesArray(otherDataForm.controls['horizontalLines'].value); let hIndex = index">
            <label>
              Yataydaki {{ hIndex + 1 }}. çizginin
              {{ hIndex === 0 ? 'dikdörtgenin sol çizgisi ile' : 'solundaki çizgi ile' }}
              arasındaki mesafe kaç mm olmalı:
              <input type="number" name="horizontalDistance_{{ hIndex }}" class="form-control form-control-sm" formControlName="horizontalDistance_{{ hIndex }}" />
            </label>
          </ng-container>
        </ng-container>

        <label for="verticalLines" class="mr-2">Dikeyde kaç adet çizgi olsun:</label>
        <input type="number" id="verticalLines" name="verticalLines" class="form-control form-control-sm" formControlName="verticalLines" />

        <ng-container *ngIf="otherDataForm.controls['verticalLines'].value > 0">
          <ng-container *ngFor="let verticalLine of getLinesArray(otherDataForm.controls['verticalLines'].value); let vIndex = index">
            <label>
              Dikeydeki {{ vIndex + 1 }}. çizginin
              {{ vIndex === 0 ? 'dikdörtgenin üst çizgisi ile' : (vIndex) + '. çizgi ile' }}
              arasındaki mesafe kaç mm olmalı:
              <input type="number" name="verticalDistance_{{ vIndex }}" class="form-control form-control-sm" formControlName="verticalDistance_{{ vIndex }}" />
            </label>
          </ng-container>
        </ng-container>
      </ng-container>

      <button type="submit" class="btn btn-primary">Gönder</button>
    </form>
  </div>
</div>
